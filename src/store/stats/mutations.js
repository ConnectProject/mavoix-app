export const setAccessToken = (state, accessToken) => {
  state.accessToken = accessToken
}

export const setConnectSessionId = (state, connectSessionId) => {
  state.connectSessionId = connectSessionId
}
